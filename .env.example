DISCORD_TOKEN=your_bot_token_here
CLIENT_ID=your_client_id_here

# Optional: TikTok authentication cookie to bypass login/captcha issues.
# Extract this from your browser while logged into TikTok.
# It should be a JSON string representing an array of cookie objects.
# Example (replace with your actual cookie data):
# TIKTOK_AUTH_COOKIE='[{"name":"sessionid","value":"your_session_id_here","domain":".tiktok.com","path":"/"}]'
# You might need other cookies like "tt_csrf_token" or others depending on TikTok's current auth flow.
# Use browser developer tools (Application -> Cookies) to find relevant cookies after logging in.
TIKTOK_AUTH_COOKIE=