services:
  monitor-bot:
    image: video-monitor
    restart: unless-stopped
    environment:
      - DISCORD_TOKEN=your_bot_token_here
      - CLIENT_ID=your_client_id_here
    volumes:
      - ./data:/app/data